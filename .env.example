# Variáveis de ambiente exemplo para deploy na Vercel
# Copie para .env.local durante desenvolvimento.

# SMTP Configuration (necessário para formulário de leads)
# Múltiplos hosts separados por vírgula (fallback automático)
SMTP_HOSTS=smtp.gmail.com,smtp-relay.gmail.com
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=seu-email@gmail.com
SMTP_PASS=sua-senha-ou-app-password

# Email Configuration
EMAIL_FROM=noreply@belzconectasaude.com.br
EMAIL_TO=leads@belzconectasaude.com.br

# Debug (opcional - 1 para ativar logs detalhados)
DEBUG_SMTP=0

# Vercel Analytics (configurado automaticamente no deploy)
# Não precisa configurar manualmente - basta instalar o pacote

# Chave de analytics (ex: Plausible, PostHog, etc.)
NEXT_PUBLIC_ANALYTICS_ID=

# Sentry / Observabilidade
SENTRY_DSN=

# Futuro: endpoint externo de CRM (ex: Hubspot, RD Station)
LEAD_WEBHOOK_URL=

# Feature flags simples (comma separated)
NEXT_PUBLIC_FEATURE_FLAGS=
